# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

A Python desktop GUI tool that generates Japan-region flight assignments for X-Plane 11/12 flight simulator pilots. It picks departure/arrival airports (random or fixed), looks up recommended AIP routes between them, and cross-references real-world flight schedules. An optional Volanta integration (F11) reads the user's flown-route history to bias random planning toward un-flown routes. The shipped artifact is `flight_dispatcher.exe` (a `flet pack` / PyInstaller `--frozen` build). User-facing strings and `README.md` are in Simplified Chinese; keep that convention when editing output.

As of v1.2.0, the code was split out of the original single 1100-line `flight_dispatcher.py` into a small package: `flight_dispatcher.py` is now a **thin entry shim**, and the logic lives in the `dispatcher/` package (one module per concern — see Architecture). The front-end was a tkinter GUI from v1.3.0; **as of v2.0.0 (F25) it is [Flet](https://flet.dev) — Python API, Flutter renderer** (`dispatcher/ui_flet/` → `run_flet()`). The entry shim just does `from dispatcher.ui_flet import run_flet; run_flet()`. **tkinter is gone** (`gui.py` deleted), and with it the only two third-party deps of the v1.x era (`tkintermapview` + `Pillow` — Flet draws map markers with ordinary controls, no bitmaps needed). The 17 logic modules stay **pure-stdlib and zero-GUI**; only the front-end needs `flet` + `flet-map`.

## Commands

No build system, no pytest suite (headless smoke scripts live in the session scratchpad, not the repo). Deps: `pip install flet flet-map` (Python ≥3.10; the 17 logic modules themselves are stdlib-only).

- Run from source: `python flight_dispatcher.py` → Flet window (the only front-end; there is no CLI/headless mode).
- Build the distributed exe: `flet pack flight_dispatcher.py -n flight_dispatcher -y --pyinstaller-build-args="--exclude-module=numpy" ...=scipy ...=tkinter ...=PIL` → **onefile ~54 MB, cold start ~2.3s**. `flet pack` wraps PyInstaller and pulls in the prebuilt Flutter client (**no Visual Studio, no Flutter SDK needed**; `flet build` would need both — don't use it). The excludes matter: numpy alone is +25 MB of dead weight. `--pyinstaller-build-args` must use the `=` form — the space form gets swallowed by argparse.
- Frozen mode is detected via `sys.frozen`; `get_real_run_path()` returns the **exe's dir** (not `_MEIPASS`) — verified under `--onefile`, so `NavData/` and the sibling json files resolve as always.
- The UI redirects `sys.stdout`/`stderr` into its log pane (`controller.LogSink` → `Shell.log_emit`), installed **before any logic call** — which is why `--noconsole` (where `stdout is None`) is safe for all the reused functions that `print()`.

## Architecture

**Three layers**, with a hard rule between them: **the 17 logic modules import no GUI framework, and `controller`/`viewmodel` import no GUI framework either.** Only `ui_flet/` knows Flet exists.

1. **Logic** (17 modules below) — pure stdlib, zero GUI.
2. **`controller.py`** (orchestration: `AppState` / `init_app` / `plan` / `compute_proc` / `volanta_sync` / `LogSink`) + **`viewmodel.py`** (per-screen pure-data Models: `result_spans` / `map_model` / `ProcPanelModel` / `AipTableModel` / `OpsEditorModel` / `MultiSelectModel` / `enabled_controls`…). Both are **headless-testable** and are the behavior contract the UI renders. `controller` never marshals threads — it only raises, returns, and invokes callbacks; marshalling is the UI's job.
3. **`ui_flet/`** — render only: build/read/write controls + bind events. Zero business logic.

One module per concern:

| Module | Responsibility |
| --- | --- |
| `dispatcher/model.py` | `Airport` data model |
| `dispatcher/config.py` | `get_real_run_path` (sibling-file anchor), `list_drives`, `installed_scenery.json` read/write |
| `dispatcher/navdata.py` | bundled `NavData` discovery (`find_navdata_file`) + `check_airac_currency` + `locate_xp_root` (功能 A) |
| `dispatcher/scenery.py` | multi-source scenery scan (XP `Custom Scenery` + MSFS `Community`, 功能 B) |
| `dispatcher/data.py` | `load_airports_from_navigraph`, `load_japan_icao_set`, `load_aip_routes_from_csv` |
| `dispatcher/volanta.py` | Volanta flown-route reading + preference-gated browser sync (F11) |
| `dispatcher/flightaware.py` | aircraft/time matching helpers + FlightAware scrape |
| `dispatcher/airlines.py` | loads `airlines.json` (data/logic split) → `pick_sim_airline` (route-plausible sim callsign); auto-creates the file from built-in defaults, falls back to them if it's missing/corrupt |
| `dispatcher/aircraft.py` (F17, **v1.4.0 🚧**) | loads `aircrafts.json` (SimBrief's 212-type DB, slimmed 909KB→34KB with private `airframes`/`user_id` stripped) → `find_aircraft_id` (exact id/icao → `_ALIAS` disambig → name/search → numeric) + `aircraft_choices` for the GUI combobox. SimBrief's `type` param uses `aircraft_id` (unique; bizjets/freighters share ICAO but differ by id, e.g. B738 vs BBJ2) |
| `dispatcher/routing.py` | `calculate_distance_nm`, `find_aip_route`, `get_random_route` (incl. the `require_both_scenery` filter) |
| `dispatcher/planner.py` | `build_flight_plan` → `FlightPlan` (compute: FlightAware scrape + sim-callsign + **SimBrief prefill URL** F16), used by the GUI; `parse_runway_ft`/`parse_dist`; `_build_simbrief_url`/`_normalize_actype`(→aircraft DB)/`_split_callsign`. `FlightPlan` carries `generated_route`/`_warn` (F15), `generated_route_dist`/`aip_route_dists` (F19), `simbrief_url` (F16), `aip_maps`/`gen_map` (F18 per-route map coords), **`sb_base`** (F20 base params) + public `simbrief_url(sb_base, route)` to rebuild the link after SID/STAR selection. The F16 result-card SimBrief link defaults its `route` to the **displayed** route (`generated_route`, else first AIP row via `_first_aip_route_str`) so the link matches what's shown — an empty `route` would make SimBrief auto-generate a different one. **v1.6.0**: `_build_simbrief_url`/`simbrief_url` take `eobt_utc_min` → append `deph`/`depm` (the proc panel's EOBT, JST→UTC) |
| `dispatcher/procedures.py` (F20, **v1.4.1 🚧**) | CIFP runway/SID/STAR enumeration + endpoint pre-filter. `_parse_runways`/`runway_length_ft` (length from the two reciprocal thresholds' haversine — CIFP length field unreliable)/`runway_heading_deg` (=runway#×10). `enumerate_procedures` → per-proc `runways` (`RW34B`→34L+34R), `trans` **{name:(first_fix, term_fix)}**, `body_first`/`body_term`. **`matching_choices`** pre-filters the runways + `SID.TRANS`/`STAR` that actually connect to the route endpoint: **match the transition's endpoint** — dep(SID) joins the transition's **terminal** fix (=the TRANS), arr(STAR) joins its **first** fix; bare proc connects the transition's other end (or body endpoint if no transition). *(Fixes the F20 bug where matching against every fix a transition passes through — incl. its IF-start — mislabels e.g. RJCC `TOBBY`→`X.BUTOS` instead of bare SID.)* **When an airport has no SID/STAR at all** (many have only IAP/radar vectors, e.g. RJER), it falls back to listing **all physical runways with empty labels** so the user can still pick a runway — don't drop runway selection just because there's no procedure (v1.4.1-alpha4). Full-route preview coords: `_load_fix_index`/`_resolve_fix` (parse full `earth_fix`+`earth_nav` incl. terminal/section-P waypoints), `procedure_coords`/`full_route_coords` (SID+enroute+STAR). Section E∪D are connection points (many endpoints are VORs). Lazy per-airport cache. **F23**: `enumerate_approaches(icao)` parses `APPCH:` records → per-approach `{ident,type,suffix,runway,name,trans}` (net-new — only this + `router._cifp_endpoints`' fix-parsing touch APPCH). Groups by coded ident `p[2]` (`I16L`→ILS RWY16L, `X22-W`→LDA W RWY22, `I34LX`→ILS X RWY34L; circling e.g. `VORA`→raw ident, no runway); type from ident's leading letter via `_APPCH_TYPE`; runway from `_RWNUM_RE`; suffix = whatever trails the runway (hyphenated `-W` or appended `X`). CIFP has no plain-language names — `name` is synthesized. Feeds the ops-editor IAP list. **v2.0.0_alpha2**: 衔接判据抽成**唯一真源** `_connect_points(d, is_dep)` → `(过渡衔接点, 裸程序衔接点)`，`matching_choices` 与新的 **`star_connect_points(icao)`**（router 用它判断「enroute 该在哪儿收手交给 STAR」）**同吃一份**——两边各写一份正是 RJAH 默认选错 STAR 的病根（router 的 `_cifp_endpoints` 把过渡沿途每个点都算进场点，比 `matching_choices` 的窄口径宽，于是 enroute 被拼过了 STAR 的入口）。回归不变式：69 机场 / 183 个衔接点，`star_connect_points` 里每一个都必须能被 `matching_choices` 匹配到。另新增 **`_label_order`**：端点匹配不上而回退「列出全部程序」时，按**接入点离来向航路的远近**排序（`_ORDER_NEAR_FIXES`=6），**不再按字母序**——面板默认选的就是第一个（viewmodel `labels[0]`），字母序会把反方向那条排前面（RJAH 有 GOT1 与 TATSU 两条 STAR，从关西飞来默认套了往北绕出去再折回的 GOT1） |
| `dispatcher/operations.py` (F23 v1.5.0 · **apply engine v1.6.0**) | `operation.json` (run-dir, `get_real_run_path()`-anchored; **tracked + shipped static data** like `transfer_points.json` — the repo/release carry a full RJTT Haneda ruleset as the sample/default; the editor rewrites it in place) load/save/normalize for per-airport **operation rules**. `load_operations()` (missing/corrupt → `{}`, never auto-writes a default), `save_operations(data)` (atomic temp→`os.replace`, `_prune` drops rule-less airports + injects `_comment`), `airport_rules(data, icao)`/`airports(data)`. Schema: `{ICAO:{rules:[{name, cond:{time_jst:["1500-1900"], days:[3,6], ref_runway:"RW34L", wind_kind:"tailwind", wind_min_kt:10, ceiling_min_ft:1500, ceiling_cover:"SCT", visibility_min_m:6000}, dep:{runways,sids}, arr:{runways,stars,iaps}}]}}` — dep/arr split (SID=dep, STAR/IAP=arr); runways `RW..`, sids/stars = CIFP proc names, iaps = coded idents. `days` = ISO weekdays 1=Mon…7=Sun the rule applies to (empty=every day; late-night runway ops differ by weekday). Condition gates (all optional; a rule matches when time AND weekday AND wind AND weather gates hold; a gateless side is the default for that dimension): **(1) Wind gate** = the `wind_kind` (`tailwind`顺风/`headwind`逆风/`crosswind`侧风) component ≥ `wind_min_kt` relative to `ref_runway` (real ops "switch when tailwind hits N kt"; 都心運用 = `crosswind@RW16L≥15` → 16L/16R crosswind exceeding ~15kt switches landings to 22/23, condition references the runway being *avoided*). **(2) Good-weather gate** = ceiling ≥ `ceiling_min_ft` (ceiling = lowest layer at `ceiling_cover`=FEW/SCT/BKN/OVC-or-denser, so LDA's "few不计" = SCT-and-up counts) AND visibility ≥ `visibility_min_m`. NOT a direction sector. **Matching model (v1.6.0 apply engine): rules are EQUAL-priority — `select_rule(rules, side, ctx, rows)` selects by condition match, NOT top-to-bottom precedence, via a lexicographic tiebreak `(route-incompatible, −time/day-specificity, −satisfied-wind-gate, selected-runway-crosswind, tailwind, −has-weather-gate, list-order)`: route-compat (rule sids/stars ∩ endpoint labels) resolves 05-group vs 34R-group by route direction; specificity puts 深夜(time+days) over all-day; satisfied-wind-gate puts 南風 over gateless 北風-default; selected-runway-crosswind realizes 都心 (22 beats 16 exactly when 16's crosswind is high); has-weather-gate puts 好天 over its 恶天 twin. **Candidates whose selected runway fails `runway_ok` (tailwind>10 / crosswind>30 for the current wind) are dropped** — a downwind-超限 rule is never applied (this IS the real 北風→南風 switch: the all-day 北風 default is gateless but 34 goes 超限 in south wind so it's excluded); if none survive `select_rule` returns None and the panel keeps the airworthy wind-default. 恶天 uses the 恶天 config automatically (the 恶天 twin carries no weather gate so it survives when the 好天 gate fails — equivalent to 「从好天往下数一条」, order-independent). `evaluate_gates(cond, ctx, skip_wind=, skip_weather=)` = the four gates (weather unknown→pass=好天). `ctx={jst_min,weekday,wind,sky_layers,vis_m}`.** **F26 (v2.0.0_alpha1) — `select_rule(..., config=)`**：传入 `ntrack` 的**实测构型**（`{runways:{在用跑道}, iaps:{在用进近 ident}}`）后，匹配从「按风/天气**推断**构型」切换成「按实测构型**认**规则」：跑道对不上实测在用集合的规则直接排除；**风闸与天气闸由实测取代**（`skip_wind`/`skip_weather`——它们本就是构型的代理指标，实测过一次「顺风 9.2 节、差 0.8 节没够到 10 节门槛，于是把南風规则全闸掉，而 MLIT 明明正在跑都心運用」的荒谬情形）；**顺/侧风超限剔除也一并跳过**（实测在用的跑道就是现实，我们的风模型说它勉强也没用）；**实测进近成为好天/恶天的判别依据**（`LDA W RWY22` ⇒ 好天那条，`ILS RWY22` ⇒ 恶天那条）——比去猜云底/能见度直接得多。**时段/星期闸门仍生效**（ntrack 不告诉我们是 A/B/C 哪一档，那得靠 EOBT）。 **v1.6.0: consumed at plan time by `gui._apply_ops_rules` to preselect runway/SID/STAR/IAP (decision support — user overrides), dep matched at EOBT, arr at ETA; the F23 editor still authors/stores it** |
| `dispatcher/weather.py` (F20 · **F22 grid fallback v1.5.0**) | NOAA `tgftp` METAR + TAF (`fetch_metar`/`fetch_taf`, per-URL ~10min cache; **not** metar-taf.com which 403s) + wind math: `parse_wind`, `runway_wind` (signed headwind `spd·cosΔ` + crosswind `spd·|sinΔ|`), `runway_ok` (tailwind ≤`_MAX_TAILWIND_KT`=10, crosswind ≤`_MAX_CROSSWIND_KT`=30 — JP ops rule). Decision support, not auto-decide. **F22 grid fallback (v1.5.0)**: many small JP airports' METAR is daytime-only/absent → when missing or stale (`metar_age_sec` > `_METAR_STALE_SEC`=7200s; **this is the first use of the fetched `obs_time`**) fall back to a **synthesized standard-format METAR** built from Open-Meteo grid weather at the airport lat/lon. `fetch_grid_weather` (stdlib urllib+json, `models=jma_msm` primary — best JP-local wind/temp/cloud; only out-of-MSM-domain airports fall back to `best_match`; per-coord 30min cache), `grid_to_metar` (encode → standard METAR, **calibrated vs real RJFK**: conservative visibility = model `visibility` is unreliable so default `9999` unless WMO fog/heavy-precip code — never fabricate `FG`/`BR`; precip intensity by `precipitation` rate; drizzle 51–55 → `RA`; **layered** clouds from `cloud_cover_low/mid/high` with `///` unknown base; temp round-half-up), `resolve_airport_wx` → unified `{source:'metar'\|'grid'\|None, wind, metar_raw, metar_age_sec, taf_raw, model}` the GUI consumes at one point. Model estimate, clearly labeled non-observed in the GUI; **Open-Meteo attribution (CC BY 4.0)** printed once. `jma_msm` never outputs `visibility`/`wind_gusts` (both `null`) — handled by conservative-vis + no-gust-group. **v1.6.0 (feeds F23 apply engine)**: `parse_sky(metar_raw)`→`(layers, vis_m)` decoding cloud groups `(FEW\|SCT\|BKN\|OVC\|VV)(\d3\|///)` → `(density 1-4, base_ft)` (base ×100, `///`=None=unknown) + `_parse_visibility` (CAVOK→9999 / 4-digit m / SM); `ceiling_ft(layers, cover)` = lowest **known** base at density ≥ `cover` (LDA few不计=SCT-and-up; all-`///`→None=unknown→gate passes); `now_jst()`→`(minutes-of-day, ISO weekday 1-7)` for the time/weekday gates |
| `dispatcher/ntrack.py` (**F26 · v2.0.0_alpha1**) | 国土交通省 **ntrack**「羽田空港飛行コース」的**实测运用状况**——羽田当前实际在用的**进近方式 + 落地跑道 + 离场跑道**，每 30 分钟一条、覆盖过去 72 小时，全公开。**这是权威实测值，优先于我们靠「时段+风+天气」规则去【推断】的结果。** 取法：一次 GET `https://www.ntrack.mlit.go.jp/NtrackTop/show`，表格**内联在首页 HTML 里**（`<table id='atistable'>`），不走 ajax / 不需 JS / 不需认证（与 jp-routes、FlightAware 同族的抓取）；站点无 robots 限制。`fetch_latest(icao)` → `{time_jst, approaches, ldg, dep, raw}`（**30 分钟缓存＝它的更新周期**；失败→`None`，静默回退规则引擎）；`parse_config` 解「`<进近> LDG RWY <落地> DEP RWY <离场>`」；**`match_iaps`** 把进近串映射回 CIFP 的进近 ident——F23 合成的显示名与 MLIT 写法**逐字一致**（两边都循 ICAO 图表命名），故直接按名匹配，**唯一别名是 `RNP`↔`RNAV`**。**仅羽田**（`AIRPORTS=('RJTT',)`；成田是 NAA 另一站点，可后续接入），其余机场仍走 `operation.json` 规则引擎 |
| `dispatcher/timed.py` (F21, **v1.4.1 🚧**) | Time-based AIP-route selection (revives the reliable subset of the v1.4.0 deleted 分时段 code). `parse_time_restriction`(EOBT/ETA/compound/cross-midnight, UTC)/`route_matches_time`/`plan_times_utc` (JST−9h=UTC, ETA≈EOBT+15+dist/`CRUISE_KT`450). Dirty Altitude/Aircraft judged **against user-supplied reference values** → True/False/**None(can't tell)**, never auto-picked: `alt_matches` (handles `FLxxx±`/`FLxxx-FLyyy` ranges — user-given FL makes ranges tractable), `aircraft_matches` (JET/PROP; geographic/airport conditions→None), `filter_candidates`→`match|no|unknown`, `resolve_unique`, `describe_restriction` (→JST + 日间/夜间 label) |
| `dispatcher/router.py` (F15, **v1.4.x 🚧**) | `generate_route` — local A* airway route generator over bundled navdata (`earth_awy/fix/nav` + `CIFP/`), used when no AIP route exists. Japan-bbox airway graph (lazy/cached); A* between **endpoints = learned dep/arr transition fixes ∪ VATJPN transfer gates ∪ CIFP** (union + `_dir_filter`, A* self-selects best); `_trace_airway` + `_parse_aip_route(densify=True)` walk each airway to insert intermediate fixes (precise along-airway distance + dense map); `route_geometry`/`route_length_nm` expose coords & length. **Route-quality (续作2)**: A* prefers RNAV airways (`_TRAD_AIRWAY_PENALTY` + `_pick_airway`/`_is_rnav`) and high-frequency real corridors (`seg_pop` weighting `_TRUNK_POP`/`_OFFTRUNK_PENALTY`). **续作3 (AIP-bridge × corridor fusion, v1.4.1_alpha2)**: `_learn_routes` (was `_learn_airway_directions`, lazy `_ensure_directions`) learns from `routes.csv` in one pass → `legal_seg`/`oneway` (one-way RNAV not labelled reversed; earth_awy marks shared one-way segments `N`) + `seg_pop` (corridor popularity) + **`dep_heads`/`arr_tails`** (each airport's real transition fixes — only when the route string's first/last token is itself a fix); `_departure_candidates`/`_arrival_candidates` union those with transfer gates + CIFP; **on-field VOR fixed** (`_onfield_vor` = nearest section-D VOR ≤`_ONFIELD_VOR_NM`=15nm, not any section-D VOR); **Rule-5 AIP bridge (`_try_aip_bridge`) removed** — learned endpoints make direct A* theoretically optimal, the bridge borrowed neighbor-airport departure fixes causing backtracks. `_dir_filter` + departure quality give-up (`_GATE_GIVEUP_RATIO`, `_direct_route._run`); `DEBUG_CORRIDOR` flag logs corridor breakdown. **Arrival tail (v1.4.1)**: `_append_arrival_tail`/`_arrival_tail_keys` append the VATJPN「到着」post-gate **DCT** fixes (`transfer_points.json` `arr_dct`, e.g. RJFM north `KUE ESKAP KROMA ENBEN MZE` — these are off-airway so A* can only reach the gate) to the generated route, with a backtrack trim (`_ARR_TAIL_BACKTRACK_NM`) that drops tails heading away from the field. **Arrival corridor reward (v1.4.1-alpha4)**: `_arr_dct_anchors` re-anchors an arr_dct entry whose gate is an off-network terminal fix (RJFF `HABOH`, region RJDA, no graph node) to the first reachable fix in `[gate]+tail` (→`FUGEN`), keeping that VATJPN arrival reachable as a fallback; `_prefer_corridor_arrival` (end of `_direct_route`) then swaps A*'s shortest arrival endpoint for the **most-flown direction-appropriate learned gate** (`arr_tails` frequency) when its route is ≤`_ARR_CORRIDOR_TOL`=1.15× the shortest AND that gate is >`_CORRIDOR_FREQ_RATIO`=1.5× more flown than the current endpoint's actual final fix — so RJKN→RJFF rides `Y25 ISKUP` not near-field DGC, while the freq guard keeps RJTT→RJOO on the official IKOMA (not another direction's globally-hotter IZUMI). `_run(use_dep_gate, exits=)` is parametrized so A* can be forced onto a specific gate. **v2.0.0_alpha2 — 六处航路生成修复（全部有 200 对回归佐证）**: (a) **A\* 转弯代价** — 状态由 `key` 改为 `(prev, key)`，超出 `_TURN_FREE_DEG`=30° 的部分按 `_TURN_NM_PER_DEG`=0.30 折算等效海里（节点态 A* 里掉头是**免费**的，这是 RJOA→RJTH 100° 发夹弯的根源）；罚项 ≥0 故 h=大圆仍可采纳。 (b) **`_enroute_entry(graph, toks, inbound=, skip=)`** — 端点学习取「第一个真正落在航路网上的 token」，且**两端判据反向**：离场看**出**边、进场看**入**边（进场门常是航路**终端**：成田 RUTAS/SUPOK/LUBLA 出边=0 → 旧代码整段跳过、退到上游过境点 UTIBO，**34 条官方航路用的 RUTAS 一条没学到**；修后 79/79 机场的官方末点全部学到）；`skip=_arr_dct_only(arr)` 跳过 VATJPN 明列的**门后径路点**（`YAMGA KUE ESLIL HIGOH KGE` 的门是 KUE，`ESLIL HIGOH KGE` 是进场径路，扫进去会学出终端区里的假门）。 (c) **走廊守卫** `_min_seg_pop` — `_smooth_airway_continuity` 不得拿在飞的走廊换从没人飞的平行航路；且平滑器不再越权改航路名（名字交给 `_format_route` 按图里的真实边重建）。 (d) **`_prefer_star_handoff`**（`_STAR_HANDOFF_TOL`=1.20）— A* 的代价含「落点→机场」直线段，于是**本场 VOR**（代价≈0）结构性压过一切真进场门（RJNA 的 KCC 离场 0.8NM，从任何方向来都赢，而 RJNA 的 5 条 STAR 没有一条从 KCC 起）→ 容差内改落到能**交接 STAR** 的点。 (e) **`_via_dir_ok`/`_via_anchors`/`_radj`**（`_VIA_DIR_TOL_DEG`=90°、`_VIA_DIR_MIN_NM`=100）— 按 VATJPN 的**官方上游走廊**过滤进场门（门本身的位置不编码来向，编码来向的是它上游那段走廊）；短于 100 NM 的航段不过滤（全程都在终端区，来向无意义——实测 32/47 NM 的航段夹角 124°/92°，会误剔官方门）。 (f) **`_maybe_dct`**（`_DCT_MAX_NM`=250、`_DCT_GIVEUP_RATIO`=1.5）— 短程上端点候选会退化（离场门把你往反方向拽、进场门却贴在出发机场旁边），A* 只能「先飞出去再飞回来」（RJDU→RJDO 大圆 **45 NM** 却飞 174 NM；RJSR→RJCB 在 UWE **174° 掉头**）→ **降级为直飞(DCT)并在结果卡明确标注**，与官方 AIP 对短途航段的写法一致（61 条纯 DCT 行全是这类）。200 对：可疑航路 **9→4**、总距 **−665 NM**。**已知残留**：`_nearest_nodes` 的 `_MAX_ENTRY_NM`=120 偏大（无官方端点的军用/小场会取到 100+ NM 外的网点当进场点），但收紧半径实测**净亏**——根因在 A* 把「端点→机场」当直线 DCT **白算**，留待后续 |
| `dispatcher/controller.py` (**v2.0.0**) | 编排层，**零 GUI 依赖**（既不 import tkinter 也不 import flet）。`AppState`(dat_path/scenery_map/aip_data/aip_index/flown_counts/volanta_auto/volanta_meta) · `NavDataMissing` · `LogSink`(file-like，`emit` 由 UI 提供并负责 marshal) · `init_app()` · `plan(state, fields) -> (FlightPlan, proc)` · `compute_proc(...)`（**v2.0.0_alpha2**：每条候选多带一个 `arr_gate`＝航路末点＝本次实际用的**进场门**，面板据此查 VATJPN 的「门↔STAR 配对」；`generated_warn` 改为**只要 router 给了 warn 就显示**——两种：①含大角度转弯 ②短程已降级为直飞，且 router 出的是**完整成句**的提示，UI 不再自己拼后缀） · `volanta_sync(cancel_evt, on_status, on_popup) -> 'synced'\|'cancelled'\|'timeout'\|'error:…'` · `reload_volanta` · `set_auto_sync`。**铁律**：只抛异常 / 只返回值 / 只回调，绝不碰控件、绝不跨线程 marshal（那是 UI 的活） |
| `dispatcher/viewmodel.py` (**v2.0.0**) | 各界面的**纯数据 Model**，**零 GUI 依赖**、可 headless 单测——这是两套 UI 之间唯一的行为契约（也是当初 tk→Flet 能逐字对拍的原因）。`Span`+`result_spans(plan)`（结果卡 = span 列表，UI 只把它画成具体富文本）· `map_model(coords)`（折线 + 三档 marker + bounds）+ `_fit_zoom` · `AircraftModel` · `runway_items`/`wind_desc`/`wx_text` · `eobt_*` · **`ProcPanelModel`**（F20/F21/F24 整套状态机；**v2.0.0_alpha2** 新增 `_default_proc`：到达侧优先用 **VATJPN 的「门↔STAR 配对」**预选 STAR（`router.gate_stars(icao, gate)`），没有配对才退回端点预筛的首个候选——而那个「首个」已由 `procedures._label_order` 按**来向**排过序，不再是字母序）· **`AipTableModel`**（F21 表 + 严格判定）· **`OpsEditorModel`**（F23 整套状态机：载入/切机场/增删改/复制/重排/脏标记/静默提交/保存/多机场隔离）+ `MultiSelectModel`（`_all`/`_sel`/`_shown`——「过滤时隐藏的已选项不丢」）· `form_to_rule`/`rule_to_form`/`move_rule` · `enabled_controls`（控件启停规则是数据，映射到具体控件由 UI 做） |
| `dispatcher/ui_flet/` (**F25 v2.0.0**) | Flet 前端（Python 写、Flutter 渲染），**只剩渲染**。`shell.py` = **唯一允许碰 `page` 的模块**：`run_bg`=`page.run_thread`、**`post`=`page.run_task(协程函数,*args)`**（THE marshal point——`page.update()` **非线程安全**，跨线程必须经这里；注意 `run_task` 收的是协程**函数**不是协程对象）+ 日志桥（加锁缓冲 + 只调度一次 flush = 一批一个 patch；thread-local 重入守卫，因为 flet 自身 logging 写 stderr 而 stderr 已被我们接管）。`theme.py`（语义色 + `panel()`）· `app.py`（`DispatcherApp`/`run_flet`，接线 controller↔视图）· `form_view` · `result_view`（span→`ft.TextSpan(on_click=闭包)`，干掉 tk 的 `_map_tags` 簿记）· `log_view` · `proc_view`(F20/F21/F24) · `aip_dialog`(F21) · `ops_view`(F23) · `map_view`(F18)。**Flet 无多窗口** → 地图与规则编辑器是 pushed `ft.View`（不是 Toplevel），F21 是 `AlertDialog(modal=True)`；一次规划的多条航路做成**地图内的标签页**（tk 版「并排开多个地图窗口」是本次迁移**唯一的永久功能倒退**） |

**Import-path note:** `dispatcher/config.py:get_real_run_path()` anchors every sibling file (`NavData/`, `installed_scenery.json`, `routes_cache.csv`, `volanta_data.json`). In source mode it returns the **parent of the package dir** (`dirname(dirname(__file__))` = project root), NOT the module's own dir — so the package layout resolves the same root the old single file did. Frozen mode still uses `sys.executable`'s dir. Don't "simplify" this back to a single `dirname`.

Data flows: locate navdata (bundled `NavData/` only) + AIRAC check → scan installed scenery (XP + MSFS) → load AIP routes → (optionally sync & load Volanta flown routes) → each **规划航线** click weight-picks/matches airports by distance, finds AIP routes, and scrapes real flights.

**Navdata discovery** (`find_navdata_file`): finds `earth_aptmeta.dat` in the **bundled `NavData/` folder** next to the program (primary and only source; fully XP-decoupled, so MSFS-only users without X-Plane work too). If it's absent the GUI shows a messagebox pointing to Navigraph (`navigraph.com/downloads`, "X-Plane 12"). The old CLI had extra fallbacks — exe-sibling `.dat`, X-Plane install auto-locate, manual paste — bundled in `find_xp_data_files`, which was removed with the CLI in v1.3.1 (`locate_xp_root` itself stays, used by the scenery scanner). `scenery_packs.ini` is now **optional** — scenery detection moved to the scanner below. `check_airac_currency` reads `NavData/cycle_info.txt` and warns (with month-abbr parsing that doesn't depend on locale) if the AIRAC cycle has expired. `get_real_run_path()` anchors all sibling files (`installed_scenery.json`, `routes_cache.csv`, `volanta_data.json`).

**Airport loading** (`load_airports_from_navigraph`): parses the whitespace-delimited `.dat`, keeping only ICAO regions `RJ`/`RO` (Japan) with runways ≥ `min_runway_ft`. Marks `is_military` from a type flag and sets `scenery_sources` (a set like `{'XP','MSFS'}`, or `None` if scenery wasn't detected) from the scanner's `scenery_map`; `has_scenery` is a derived property. Each row becomes an `Airport` object (code + decimal lat/lon).

**Multi-source scenery scan** (`scan_installed_sceneries` + helpers, F5): detects installed airport scenery across **both sims**, returning `dict{ICAO: set('XP'|'MSFS')}`. XP: `locate_xp_root` → `Custom Scenery`, each pack's `Earth nav data/apt.dat` parsed for airport-row ICAOs. MSFS: `find_msfs_packages_dir` reads `InstalledPackagesPath` from candidate `UserCfg.opt` paths (covers 2020/2024 × Steam/MS Store, with a drive-scan fallback) → `Community`; per pack `_extract_msfs_pack_icaos` runs a 4-step cascade (folder name → `ContentInfo/…/ContentHistory.json` `type=="Airport"` content → `scenery/*.bgl` filenames → give up), pre-filtered by `manifest.json content_type=="SCENERY"`. Only `RJ/RO` ICAOs are kept, then **whitelisted against the real-airport ICAO set from navdata** (`load_japan_icao_set` parses every `RJ/RO` code from `earth_aptmeta.dat`): this drops false positives where the `R[JO]xx` extractor matched an ordinary word inside a folder/bgl name (e.g. `ngt-road-mesh`→`ROAD`, `aerocaches`→`ROCA`) — a real airport set is the only thing that distinguishes `ROAD` (not an airport) from `ROAH` (Naha). The extractor regex (`_extract_japan_icaos`) is also word-boundary-anchored as a first line of defense; the navdata whitelist is the authoritative one (skipped if navdata is missing, to avoid wiping everything). Results + a lightweight fingerprint (`{scenery_dir: {pack: mtime}}`) cache to `installed_scenery.json`, so unchanged dirs return instantly (a stale cache that still holds pre-fix false positives is self-healed/rewritten on the next cache-hit read); a changed pack-set/mtime triggers a rescan. The same JSON also holds `xp_root`/`msfs_packages` (migrated from the legacy `xp_path_config.txt`). MSFS recall isn't 100% (manifest has no ICAO field; oddly-named packs without `ContentHistory.json` are missed) — it's a soft "reference" hint.

**AIP route data** (`load_aip_routes_from_csv`): network-first — downloads `https://jp-routes.vercel.app/public/routes.csv` (5s timeout), refreshing the local `routes_cache.csv` mirror. Falls back to the cache offline (warns past a 28-day staleness window). The CSV columns are `DEP,DEST,Time Restriction,Altitude,Aircraft,Route,Remarks`; `find_aip_route` filters rows by exact dep/dest ICAO match.

**Real-flight scraping** (`fetch_real_flights_with_filter`): HTTP-GETs a FlightAware `findflight` page and regex-extracts an embedded `FA.findflight.resultsContent` JSON array. Filters by airline ICAO prefix, aircraft type (fuzzy via `is_aircraft_match`), and departure time window. Returns up to 5 matches; if none match the filters, returns up to 5 unfiltered alternatives; if the scrape returns **zero** flights, the caller falls back to a sim callsign whose airline is chosen by `pick_sim_airline(dep, arr)` (`dispatcher/airlines.py`) — a route-aware pick that only allows airlines whose network covers both endpoints' geographic regions, so a Hokkaido-based carrier (ADO) won't be assigned a Fukuoka–Okinawa route (a user-specified airline still wins). The airline→region networks and airport→region map live in an editable `airlines.json` (run-dir, auto-created from `_DEFAULT_DATA` on first run; corrupt/missing → built-in defaults), keyed by airline ICAO so users can add/remove/edit carriers (new airlines, VATSIM virtual airlines) without touching code. This depends on FlightAware's page structure — the regex (`FA.findflight.resultsContent = [...]`) is the fragile coupling point.

**Volanta flown-route reading** (`load_volanta_flown_routes` + helpers): gets the user's Volanta (`fly.volanta.app`) flight history to bias random planning toward un-flown routes (F11). The data comes from the **`/api/v1/Flights` API**, fetched directly with the user's own browser session token (see next paragraph); the parsed directed-route counts are stored in the single **`volanta_data.json`** (run dir), which also holds the sync preference and last-fetch time (it consolidates the old `volanta_config.txt`/`volanta_flights.json`/`volanta_flown.json` — first run migrates+deletes them). `load_volanta_flown_routes` reads that store, folds in any user-dropped `volanta_flights.json` (DevTools export — absorbed then deleted) and `volanta_flights.csv` (kept), and returns `dict{(dep,arr): count}`; pure stdlib, every step wrapped in try/except → empty dict on failure. `sum(counts.values())` = total flights, `len(dict)` = distinct directed routes; self-loops (`dep==arr`) are skipped. (Historical note: an earlier version regex-scanned the IndexedDB LevelDB for routes — `find_volanta_leveldb_dirs`/`extract_flown_routes` — but that was fragile (V8 dedup) and incomplete (lazy-load), and has been **removed** in favor of the API path. `_read_leveldb_text` survives, now used only to read the Local Storage LevelDB for the session token.)

**How the data is fetched — `/api/v1/Flights` via the browser session** (`try_fetch_volanta_json_via_session`): the **`/api/v1/Flights` API** returns ALL flights in one clean JSON call. The program calls it directly using the user's **own** Volanta session token read from their **own** browser — analogous to yt-dlp's `--cookies-from-browser`. Key details discovered by reverse-engineering:
- The API rejects the Firebase idToken (`iss: securetoken.google.com/volanta` → `error="invalid_token", The issuer ... is invalid`). The token it accepts is an **Orbx-issued JWT** (`iss: "Orbx"`, `aud: "regular_user"`, valid **~14 days**) — Volanta is an Orbx product. That token lives in **Local Storage** (`<Profile>/Local Storage/leveldb`), NOT IndexedDB.
- `_extract_volanta_api_token` scans the Local Storage leveldb but **only** accepts JWTs with `iss=="Orbx"` AND a `fly.volanta.app` marker within 3000 chars before them — so it never extracts/uses any other site's token from the shared Local Storage. `_fetch_volanta_flights_json` GETs the API with `Authorization: Bearer <token>` and `Accept-Encoding: gzip` (stdlib has no brotli). The token is used in-memory for that one request only — **never written to disk, never sent anywhere except `api.volanta.app`**.
- `try_fetch_volanta_json_via_session` parses the API response and merges the authoritative counts into `volanta_data.json` (atomic temp→replace, only if it parses to ≥1 route; the raw ~900KB response is NOT kept on disk). `skip_if_fresh=N` skips the network call if the store's `fetched_at` is younger than N seconds (startup uses 3600).

The GUI only auto-fetches **after the user has opted in** (see the preference model below). `_load_volanta_json` parses the file (each item is `{flight:{...}, summarisedPositions:[...]}`; route = `flight.originIcao`/`flight.destinationIcao`). When opted-in the silent fetch needs **zero** steps (no DevTools, no scrolling) as long as the user logged into Volanta in a browser within ~14 days; if the token expired it degrades to the existing accumulated store. Either way `load_volanta_flown_routes()` reads any present json/CSV/store.

**Security boundary (important for maintainers)**: this reads the user's own session token from their own browser, locally, for their own data — no third party, no exfiltration. The earlier in-session classifier block was on *exploratory* token-scanning; this is a *targeted, user-authorized* feature scoped to one app. Keep it that way: only `iss=="Orbx"` + `fly.volanta.app`-proximity tokens, only for `api.volanta.app`, never persisted/logged/transmitted.

**Persistent store + multi-source merge**: the **monotonic** flown-route store lives in `volanta_data.json`'s `flown` field (run dir) — `{"DEP|ARR": count}`, **only grows**. Merge rule: **route keys are unioned** (a flown route is never lost); **counts prefer the authoritative source** (the API fetch, or a user-dropped `volanta_flights.json`), falling back to `max()` across CSV/store for routes it lacks. The merged result is persisted back via an atomic write. For weighting only "flown (≥1) vs un-flown (0)" matters, so exact counts are non-critical — but authoritative counts keep the displayed total honest. `meta['latest']` is the store's `fetched_at` (when the API was last pulled).

**Sync preference model** (`volanta_data.json`'s `preference` field, run dir): `auto` = opted-in, anything else (missing / `ask`) = not-opted-in. The **GUI** drives it via `volanta_auto_enabled()` / `set_volanta_auto()`:
- **opted-in (`auto`)** → the init worker silently `try_fetch_volanta_json_via_session(skip_if_fresh=3600)` on startup (no prompt; **only here do we ever scan the browser's Local Storage**), then `load_volanta_flown_routes()`. Token-expired → degrades to the store. The **自动同步** checkbox shows checked.
- **not opted-in** → no startup scan; the user clicks **同步 Volanta** to sync on demand (see Browser sync below). Checking **自动同步** calls `set_volanta_auto(True)` → future startups auto-sync; unchecking writes `ask`. Either way `load_volanta_flown_routes()` still loads any manually-placed json/CSV/store.

**Browser sync** (GUI `_volanta_worker`, 5-min cap): when **同步 Volanta** is clicked and no valid token is present, `_open_volanta_in_browser()` opens **`fly.volanta.app/map`** (`_VOLANTA_LOGIN_URL`) in Edge (falls back to default browser) so the user can **log in**. A daemon thread polls `try_fetch_volanta_json_via_session()` every 3s (cancelable via `threading.Event`, `_VOLANTA_POLL_CAP`=300s) until the Orbx token is readable on disk. **Root cause of the alpha1 sync timeout (fixed 2026-06-28)**: the Orbx token IS minted right after login on `/map` (no `/flights` needed — an earlier "/map doesn't mint it" hypothesis was wrong), but Chromium flushes the renderer's in-memory localStorage to the on-disk leveldb (which we read) with a ~30s–1min lag (longer when idle, sometimes >180s) — so the old 180s poll sometimes timed out; scrolling/navigating generates localStorage writes that force an earlier flush (why the user's manual `/flights`+scroll worked). Fix: 300s cap + two `messagebox` popups (①"waiting for write" at start, ②at ~60s, guide the user to open `/flights` and refresh+scroll to force the flush) + a success popup — the program does NOT auto-open `/flights` (avoids the pre-login `/flights` hang). Plus `volanta.diagnose_volanta_session()` → privacy-safe category (`token_ok/no_dirs/empty/orbx_expired/no_orbx`, never any token value) and `try_fetch_volanta_json_via_session(diag=)` logging one fixed reason line on failure (`diag` is on only for the fast-path + startup auto-sync, NOT the 3s poll, to avoid log spam). The `_extract_volanta_api_token` proximity heuristic was verified fine and left unchanged.

**Route selection** (`get_random_route`): enumerates all airport pairs whose great-circle distance (`calculate_distance_nm`, Haversine in NM) falls in `[min_dist, max_dist]` (and, with `strict_aip=True`, exist in a prebuilt `aip_index` set; and, with `require_both_scenery=True` — the random-only GUI toggle — both endpoints satisfy `Airport.has_scenery`, which is inert when scenery wasn't detected since `has_scenery` is then `True`). Candidates are bucketed into 3 tiers by **military-endpoint count** (0 = both civil, 1 = one military, 2 = both), and selection happens within the **lowest non-empty tier** — so civil-only routes are strictly preferred and military airports (which often lack civil nav aids / approaches, so the route may be unflyable) are only picked when no civil route fits the constraints or when the user fixes a military endpoint. This matters for heavy flyers: once almost every civil route is flown, the high un-flown weight would otherwise spike military picks; the tiering prevents that (it re-flies the least-flown civil route instead). Within the chosen tier it weighted-random-picks via `random.choices` with weight `1/(count+1)²`, where `count` is the Volanta flown count — soft-prefers un-flown routes (empty `flown_counts` → uniform, matching the original behavior). Returns a 5-tuple `(dep, arr, dist, route, flown_count)`. (This replaced the older 150k-iteration rejection-sampling loop.)

**Startup + planning flow** (`dispatcher/gui.py`, invoked by the `flight_dispatcher.py` entry shim → `run_gui()`): a background `_init_worker` does the one-time setup (navdata discovery + AIRAC check + scenery scan → `scenery_map` + airline data + AIP load + preference-gated Volanta sync + prebuild `aip_index`), posting results back to the Tk thread via `after()`. Each **规划航线** click runs `_plan_worker` on a daemon thread: airports are *reloaded* every time via `load_airports_from_navigraph` (passing `scenery_map`) because `min_runway_ft` is a per-plan input, then route selection → `planner.build_flight_plan` → `FlightPlan`. `_render_plan` renders it into the result widget — scenery labels via `Airport.scenery_label()` (`[地景:XP/MSFS/XP+MSFS]` or `[⚠️无地景]`), plus a `已飞过 N 次` note when `flown_count > 0`. The compute (FlightAware scrape + sim-callsign) lives in `planner.build_flight_plan`, decoupled from rendering so a future GUI-framework swap only touches `gui.py`.

**Local A\* route generation** (`dispatcher/router.py`, F15, **v1.4.x in progress**): when no direct AIP route exists, generate an enroute airway route from the bundled X-Plane navdata. Priority chain: **Rule 0** (direct AIP) is the app-level gate (`gui._plan_worker` calls `find_aip_route` first; `generate_route` is only invoked when it returns None). Inside `generate_route(dep, arr, dat_path, aip_data, airports)` (`airports` now unused, kept for back-compat): it builds a lazy/cached `AirwayGraph` (parse `earth_fix`/`earth_nav`/`earth_awy`, Japan bbox `(24,46,122,149)`, ~2406 nodes/4582 edges, key `(ident,region)`, honoring airway direction N/F/B), then `_direct_route` runs **A\* between endpoints** chosen by `_departure_candidates`/`_arrival_candidates`: the **union** of (i) **learned dep/arr transition fixes** from `routes.csv` (`graph.dep_heads`/`arr_tails`), (ii) **VATJPN transfer-point gates**, and (iii) **CIFP** procedures — departure = SID exits (section `E`) ∪ **on-field VOR** (`_onfield_vor`: nearest section-D VOR within 15nm — NOT any section-D VOR, which would grab a mid-SID/feeder VOR like RJSY's YTE at 37nm), arrival = STAR entries → on-field VOR → IAF/IF — all `_dir_filter`'d (direction) + connectivity-filtered (on the airway network), geometric nearest-toward as last fallback; A\* self-selects the best start/exit. A continuity check flags enroute acute turns (`_MAX_TURN_DEG`=100°) → `FlightPlan.generated_route_warn`. Result is rendered as a distinct "🧭 生成航路 (非官方 AIP)" section. *(The old **Rule 5 AIP bridge** `_try_aip_bridge` was removed in v1.4.1_alpha2 — see 续作3 below.)* Domain rules in the `route_planning` skill (`.claude/skills/route_planning/`); full design in `revisions.md` v1.4.0/续作3.

**Route quality (续作2)** — layers that make generated routes match real-world operation, all learned from `routes.csv` (official published routes never fly an airway illegally): (1) **RNAV preference** — A\* penalises pure-traditional edges (`_TRAD_AIRWAY_PENALTY`=1.15) and `_format_route`/`_pick_airway` prefer RNAV names (Q/T/Y/Z·L/M/N/P); RNAV share 74→92%. (2) **Airway-direction learning** — `earth_awy` marks a one-way RNAV airway `N` where it shares a physical segment with a two-way airway, losing its direction; `_learn_routes` walks every `routes.csv` route → `legal_seg{(u,v):airways}` so `_pick_airway` only labels a one-way RNAV in its attested direction (fixes Y284/Y43/Y312 reversals); label-layer only, graph topology unchanged. (3) **Corridor weighting** — same learning yields `seg_pop` (per-directed-segment popularity); A\* prefers trunk corridors (`_TRUNK_POP`/`_MINOR_CORRIDOR_PENALTY`/`_OFFTRUNK_PENALTY`), all multipliers ≥1 (admissible); trunk share 73→80%, +0.4% distance. (4) **Transfer-point endpoints** — `transfer_points.json` (VATJPN「移管点と高度」SOP, 56 airports' arrival/departure gates) loaded by `_transfer_points()`, `_dir_filter`'d (drop off-direction gates, `_GATE_DIR_MARGIN_NM`=30) + departure quality give-up (`_GATE_GIVEUP_RATIO`=1.4: if the gate route is suspect or ≫GC, drop it → on-field-VOR/geometric, i.e. "本场台离场+VOR 程序").

**AIP-bridge × corridor fusion (续作3, v1.4.1_alpha2)** — the insight that endpoint-learning subsumes the old AIP bridge: (1) **Endpoint learning** — `_learn_routes` (was `_learn_airway_directions`) in the SAME `routes.csv` pass also extracts each airport's **real transition fixes** → `graph.dep_heads`/`arr_tails` (e.g. RJGG-north departs KCC, RJFM-east departs MADOG); only when the route string's first/last token is itself a fix (airway-leading rows like `Y14 HWE …` would otherwise pick a far-downstream fix → degenerate "DCT 200nm to a midpoint", e.g. RJCN→RJSM collapsing to `HWE`). `_learned_heads`/`_learned_tails` read them. (2) **Union endpoint selection** — `_departure_candidates`/`_arrival_candidates` union learned ∪ transfer-gate ∪ CIFP (dir-filtered), A\* self-selects (monotonically ≥ any subset, so strictly non-worse than CIFP-only). (3) **On-field VOR fix** — `_onfield_vor` = nearest section-D VOR within `_ONFIELD_VOR_NM`=15nm; the old code treated *every* section-D VOR as the on-field VOR, wrongly picking mid-SID waypoints (RJSY's YTE, a ZUNDA2 waypoint 37nm out) — this matches the `route_planning` skill's own `cifp_format.md` rule. (4) **Rule-5 AIP bridge removed** — `_try_aip_bridge` + constants `_BRIDGE_TOLERANCE`/`_OVERSHOOT_NM`/`_BRIDGE_HEAD_NM` deleted; with correct learned endpoints the direct A\* is theoretically optimal, and the bridge's borrowing of a *neighbor airport's* departure fix caused backtracks (RJFM→RJBE wrongly via Kagoshima's MIDAI). 50-sample after removal: total distance dropped 22832→22682nm, trunk 78%, 0 degenerate routes. All multipliers/penalties are tunable module constants. `transfer_points.json` is run-dir-anchored (ship next to the exe in frozen builds, like `airlines.json`/`aircrafts.json`). `DEBUG_CORRIDOR=True` logs each route's corridor breakdown. *(The skill's `SKILL.md`/`enroute.md` still describe a "case 5" bridge — a valid domain concept, but no longer this project's implementation; pending cleanup.)*

**v1.4.0 续作 — SimBrief / aircraft DB / interactive map (F16–F19, full design in `revisions.md` v1.4.0 续作)**:
- **SimBrief one-click dispatch (F16)**: `planner._build_simbrief_url` builds a SimBrief *custom-options* prefill URL (`dispatch.simbrief.com/options/custom?orig=&dest=&type=&airline=&fltnum=&route=`); the user's **own browser SimBrief session** generates the OFP — no credentials, publishable (unlike calling the API with my Navigraph sub). `route=` empty → SimBrief auto-routes (filled by the timed-route feature later). The earlier API path was abandoned (Navigraph token lives only in browser JS memory).
- **Aircraft DB (F17)**: `aircrafts.json` is the slimmed SimBrief type DB (private fields stripped — **must not be re-overwritten with the raw scrape**, see `.gitignore`). `aircraft.py` maps user input → `aircraft_id`; the GUI aircraft field is a searchable Combobox.
- **Interactive map (F18, first third-party dep)**: `gui._open_map(coords,title)` pops a `Toplevel` with a real OSM map (`tkintermapview`, drag/zoom), 3-tier markers (airport/waypoint/intermediate) + polyline. **Each route** (every AIP row + the generated one) gets its own 「🗺️」 link via dynamic `maproute_N` tags → you can open several windows (one per time-split route).
- **Route distance + densification (F19)**: `_parse_aip_route(densify=True)` Dijkstra-walks each airway to insert intermediate fixes, so distance is summed along the real airway (not waypoint-to-waypoint straight lines) and the map is dense (RJFR→RJEC 11→21 pts). Shown as 「航路长 X NM (较大圆 +Y%)」.
- **⏸️ Deferred: time-based routing → SimBrief `route`**: `routes.csv`'s `Time Restriction` (`EOBT/ETA HHMM-HHMM`, UTC; 366 rows) would select one of several AIP routes by departure time (GUI input is **JST**, so JST−9h=UTC) + altitude/aircraft, then fill SimBrief's `route=` (covering SimBrief's time-blindness). A GUI toggle 「按真实运行时间与规则规划航路」 would drive it; `build_flight_plan(timed_route=)` / `_build_simbrief_url(route=)` are wired **and kept**. **2026-06-27: the compute layer was fully implemented in `routing.py` (never hooked to the GUI) but the per-aircraft/altitude refinement was unreliable — the `Aircraft` column is free-text applicability (`JET/DH8D/PROP` mixed with geographic `for AP west of 139E…` and airport-specific `only for RJCW` conditions), JET↔PROP relied on a hardcoded incomplete whitelist, and the `FLxxx±` single-threshold regex mis-parses `FL180-FL230` ranges — so the whole segment was deleted and deferred.** Full removed code + data analysis in `revisions.md` v1.4.0「🗑️ 已删除代码留底」. Redo direction: time is parseable; leave aircraft/altitude for the user to pick (no auto unique-selection).

## Conventions & gotchas

- All console output and prompts are Chinese with emoji prefixes; match the existing tone/format when adding output.
- External dependencies are live web services: `jp-routes.vercel.app` + `flightaware.com` (both scraped, not official APIs — can break without notice; no caching for FlightAware), NOAA `tgftp` (METAR/TAF, F20), and **Open-Meteo `api.open-meteo.com` (F22 grid-weather fallback, v1.5.0 — free JSON API, no key, stdlib urllib+json)**, and **MLIT `ntrack.mlit.go.jp` (F26, v2.0.0_alpha1 — 羽田的【实测】运用状况；抓首页内联的 `<table id='atistable'>`，30 分钟缓存；失败即静默回退 `operation.json` 规则引擎)**. Open-Meteo data is **CC BY 4.0**: keep the one-time `🌐 天气数据来自 Open-Meteo (CC BY 4.0)` attribution print (and the README credit). The grid path is only a fallback for missing/stale METAR and its output is a **model estimate labeled non-observed** — never present it as a real observation.
- `min_runway_ft` accepts user units (`1800m` or `5900ft`) and normalizes to feet; default 5900 ft.
- `installed_scenery.json` (run dir) unifies sim-dir memory (`xp_root`/`msfs_packages`) + scenery cache (`sceneries` + `fingerprint`); it supersedes the legacy `xp_path_config.txt` (migrated on first run). These config/cache files are environment-specific and overwritten at runtime. `airlines.json` (run dir) is the editable airline/region data for `pick_sim_airline` — auto-created from defaults on first run, but unlike the cache files it is meant to be hand-edited and is only regenerated if absent (a corrupt one falls back to built-in defaults without being overwritten). `transfer_points.json` (run dir, v1.4.0 续作2) holds per-airport arrival/departure gates parsed from the VATJPN transfer-of-control SOP (`route_planning` skill `transfer_points.md`); shipped static data the router reads for endpoint selection (missing → falls back to CIFP endpoints). **`arr_dct` (v1.4.1)**: per-gate post-gate **DCT arrival tails** (`{gate: [fixes]}`) — the direct fixes the VATJPN「到着」routing flies after the gate to the field, which the router appends to the generated route (see router.py row). **`arr_gates` (v2.0.0_alpha2 — 从 VATJPN「到着」栏自动推导 + `routes.csv` 实证校验，56 机场 / 206 条径路)**：`{门: [{via_fix, via_awy, dct, star, cond}, …]}`——把原来**只抽了门名、丢掉的另外两样**补回来。`…KOHWA Y546 AGPUK MIRAI ABENO IKOMA` 里三段各有其义：`KOHWA Y546`=**上游走廊(来向)** → `via_fix`/`via_awy`（供 `router._via_dir_ok` 按方向筛门；**门本身的位置不编码来向**）；`AGPUK`=移管点；`MIRAI ABENO IKOMA`=**被逐点展开的 STAR 机体** → 拿它反查 CIFP 即得 `star`=`["IKOMAE"]`（AGPUK 上同时挂着 STAR「AGPUK」与「IKOMAE」，**只有官方配对分得清**；`viewmodel._default_proc` 据此预选，多义时由跑道再分：RJFM/KUE 给 `[MELAR, KARAH]` → RW09 选 MELAR、RW27 选 KARAH）。**「展开点能否反查到 STAR」正是「官方直飞径路」与「被展开的 STAR」的判据**：反查得到 → 那是程序的活、不进 enroute；反查不到 → 真·无程序直飞径路，照抄进 enroute（RJFK `ESLIL HIGOH KGE`）。统计：唯一 STAR 11 · 多义 4 · 真直飞 30。`arr_dct` 也据此在**数据层**修正（剔除混入的 STAR 机体点，7 个机场）——修复搬回数据侧，代码层截断只作兜底。 `operation.json` (run dir, **F23 v1.5.0 · applied v1.6.0**) holds per-airport operation rules, authored via the「⚙️ 编辑机场运行规则」editor — **tracked in git and shipped in the release, like `transfer_points.json`** (NOT gitignored user data as it was in v1.5.0): the repo carries a **full RJTT Haneda ruleset (52 rules) as the sample/default** so the v1.6.0 apply engine works out of the box and doubles as a template for other airports. Users rewriting it via the editor just see a local modification. `dispatcher/operations.py` load/save + **the v1.6.0 apply engine** (`evaluate_gates`/`select_rule`) it. **v1.6.0 consumes it at plan time**: the proc panel matches current JST time (EOBT for dep, ETA for arr) + weekday + wind + weather to preselect runway/SID/STAR/IAP (decision support — user overrides).
- Scenery detection reads the sims' on-disk scenery folders (XP `Custom Scenery` apt.dat, MSFS `Community` packages), not an API — fragile to MSFS pack-naming conventions; any failure degrades to "scenery unknown" (`scenery_sources=None`, no `[⚠️无地景]` mark). Navdata ships in the bundled `NavData/` folder; `check_airac_currency` nudges the user to refresh it via Navigraph (`https://navigraph.com/downloads`, "X-Plane 12" navdata) when the AIRAC cycle expires, and the missing-navdata error in `load_airports_from_navigraph` points to the same link.
- Volanta integration reaches the user's data via Volanta's own `/api/v1/Flights`, authenticated by the Orbx session token read from the **browser's Local Storage** (Edge/Chrome/Brave only — Chromium LevelDB format; **Firefox/Safari use a different store and aren't supported yet**, a cross-platform-round item, and the path logic is Windows-centric) — not the desktop app, not IndexedDB (that old regex path is gone). Any failure degrades to disabled. All Volanta persistence is consolidated into the single **`volanta_data.json`** (run dir): `preference` (`auto` = opted-in → silent auto-sync on startup; missing/`ask` = no startup sync, use the 同步 Volanta button), `fetched_at` (last API pull), and the **monotonic** `flown` store (`{"DEP|ARR": count}`, only grows). Deleting the file fully resets both preference and flown history (there's no separate file to wipe). First run migrates+deletes the legacy `volanta_config.txt`/`volanta_flights.json`/`volanta_flown.json`. Optional user-dropped imports: `volanta_flights.json` (DevTools export — absorbed then deleted) and `volanta_flights.csv` (kept). The browser is only ever opened after the user clicks **同步 Volanta** (or has opted into auto) **and** only when no valid token is already present.
